package com.largeblueberry.bitquest.data.local

/**
 * [작업 담당자: Android 개발자 (Room Entity 설계)]
 *
 * TODO: Room Entity 클래스들 정의
 * - QuizEntity, UserEntity, QuizResultEntity, CategoryEntity
 * - 테이블 관계 설정 (@Relation, @Embedded)
 * - 인덱스 최적화 (@Index)
 * - 타입 컨버터 (List<String>, Date 등)
 * - 도메인 모델과의 매핑 함수
 */
