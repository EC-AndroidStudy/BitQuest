package com.largeblueberry.bitquest.feature_main.util

import androidx.compose.ui.graphics.Color

object BitQuestColors {
    val PrimaryGreen = Color(0xFF4CAF50)      // 메인 초록색
    val SecondaryGreen = Color(0xFF388E3C)    // 진한 초록색
    val AccentGreen = Color(0xFF81C784)       // 밝은 초록색 (액센트)
    val BackgroundGray = Color(0xFFF8F9FA)    // 배경 회색 (유지)
    val TextDark = Color(0xFF2C3E50)          // 진한 텍스트 (유지)
    val TextLight = Color(0xFF7F8C8D)         // 밝은 텍스트 (유지)
    val White = Color.White                   // 흰색 (유지)
    val WrongAnswerRed = Color(0xFFF44336)    // 오답/에러를 위한 빨간색
}